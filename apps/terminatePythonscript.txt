Add the following to the end of the python script generated by gnuradio-companion to force the script to
exit and return control to shell when key is pressed,

At top of script add:

import os

At bottom of script add:

time.sleep(1)
os._exit(0)
